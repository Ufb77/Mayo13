import { Routes } from '@angular/router';
import { InstrumentoFormularioComponent } from './instrumento-formulario/instrumento-formulario.component';

export const routes: Routes = [
    {path: 'instrumentos', component: InstrumentoFormularioComponent}
];
